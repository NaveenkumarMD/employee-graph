import { greyscale_200 } from "../../colors";

const styles = {
    container: {
        borderBottom: `1px solid ${greyscale_200}`,
        width: "100%",
        padding: 2,
    },
};
export default styles;
